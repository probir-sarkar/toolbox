import type { MergePdfSettings } from "../types";

export const DEFAULT_MERGE_PDF_SETTINGS: MergePdfSettings = {
  outputFileName: "merged-document.pdf",
};

export const ACCEPTED_FILE_TYPES = ["application/pdf"];
